name: Create Cypress report
on:
  push:
    branches:
      - 'main'
  schedule:
    - cron: '0 8 1-31 * *'
  workflow_dispatch:

jobs:
  cypress-e2e-chrome81:
    name: Run on Chrome 81
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install a project with a clean slate
        run: |
          yarn install

      - name: Run Cypress tests
        run: npm run test:chrome
        continue-on-error: true

      - name: Create unique directory
        id: create-dir
        run: echo "::set-output name=dir_name::$(date +%Y-%m-%d-%H-%M-%S)"

      - name: Copy test execution videos
        run: |
          mkdir public/${{ steps.create-dir.outputs.dir_name }}
          cp -r cypress/videos public/${{ steps.create-dir.outputs.dir_name }}/videos

      - name: Merge test results into one
        run: npm run report:merge

      - name: Generate HTML report
        run: npm run report:generate

      - name: Deploy report page
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public/${{ steps.create-dir.outputs.dir_name }}
          user_name: 'Rahul Kundu'
          user_email: 'rahulkundu2016@gmail.com'
          publish_branch: gh-pages
